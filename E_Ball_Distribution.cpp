#include <stdio.h>
int main()
{
    int t;
    scanf("%d", &t);
    while (t--)
    {
        long long n, m;
        scanf("%lld %lld", &n, &m);
        long long a[m];
        for (long long i = 0; i < m; i++)
        {
            scanf("%lld", &a[i]);
        }
    }
}